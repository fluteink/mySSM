<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fluteink.mybatis.mappers.SpecialSelectMapper">
<!--    List<User> SpecialSelect(@Param("mohu") String mohu);-->
<select id="SpecialSelect" resultType="User">
    select * from t_user where username like "%"#{mohu}"%"
</select>
<!--    void deleteUsers(@Param("ids") String ids);-->
<delete id="deleteUsers">
    delete
    from t_user
    where id in (${ids})
</delete>
<!--    List<User> searchUsers(@Param("table") String table);-->
<select id="searchUsers" resultType="User">
    select * from ${table}
</select>
</mapper>
